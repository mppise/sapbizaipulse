apiVersion: v1
kind: Service
metadata:
  name: sap-bizai-pulse
  labels:
    app: sap-bizai-pulse
spec:
  selector:
    app: sap-bizai-pulse
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
